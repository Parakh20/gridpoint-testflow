# Copy to frontend/e2e/.env. Do NOT commit the real .env file.

# A SUPERADMIN account in the demo tenant — created by the multitenancy seed migration
E2E_SUPERADMIN_EMAIL=admin@companya.com
E2E_SUPERADMIN_PASSWORD=changeme  # see migration 20260429000001 for default

# Optional override — defaults to http://localhost:8080
# E2E_BASE_URL=https://companya.optimustesting.com
