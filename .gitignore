# ── Logs ────────────────────────────────────────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Dependencies ─────────────────────────────────
node_modules
frontend/node_modules

# ── Build output ─────────────────────────────────
dist
dist-ssr
frontend/dist
frontend/dist-ssr
*.local

# ── Environment files (NEVER commit these) ───────
.env
.env.*
!.env.example
frontend/.env
frontend/.env.*
!frontend/.env.example
# mobile/.env is intentionally committed — only public values (anon key, URL)
!mobile/.env

# ── Admin bootstrap credentials (NEVER commit) ───
admin.env

# ── Supabase ─────────────────────────────────────
.branches
.temp

# ── Editor ───────────────────────────────────────
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# ── Internal docs (not needed on GitHub) ─────────
STUDY_GUIDE.md

# ── Tool runtime state (Claude Code plugins, etc.) ─
.remember/
.claude/
